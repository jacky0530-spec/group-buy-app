name: Apply combo summary display patch

on:
  push:
    branches:
      - agent/combo-summary-display
    paths:
      - 'scripts/trigger_combo_summary_display.txt'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/combo-summary-display
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply patch
        run: python scripts/patch_combo_summary_display.py
      - name: Commit changes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/pages/PendingProductReport.jsx src/components/Layout.jsx
          git diff --cached --quiet || git commit -m 'Fix combo summary grouping and styling'
          git push
